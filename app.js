//event listeners
const button = document.querySelector('button')
const element=document.querySelector('body')


button.addEventListener('click', () =>{
    element.classList.toggle("dark")//if it has the aqua calss toggle removes if it doesnt adds it.toggle is a method 
   
})